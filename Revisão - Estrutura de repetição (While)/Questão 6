#include <stdio.h>
#include <locale.h>

// 

int main () {

    setlocale (LC_ALL, "Portuguese");

    int cont = 15, soma = 0;

    while (cont <= 25) {
        if (cont % 2 == 0) {
            soma = soma + cont;
        }
    cont++;
    }
    
    printf ("%d \n", soma);

    return 0;
}
