#include <stdio.h>
#include <locale.h>

int main () {
	
	setlocale (LC_ALL, "Portuguese");
	
	int som = 50,contm;
	
	
	for (int num = 50; num <= 70; num = num + 1){
		som = som + num;
		contm = contm + 1;
	}
	printf("Soma: %d\n", som);
	printf("Média: %d", som / contm);
	
	return 0;
}
